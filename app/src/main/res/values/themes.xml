<resources>
    <!-- Base application theme. -->
    <style name="Theme.LoveLife" parent="Theme.AppCompat.DayNight.NoActionBar">
<!--        <item name="colorPrimary">@color/green_300</item>-->
<!--        <item name="colorSecondary">@color/green_200</item>-->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowAnimationStyle">@style/activityAnim</item>
    </style>

    <style name="activityAnim">
        <item name="android:activityOpenEnterAnimation">@anim/slider_in_right_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/slider_out_left_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/slider_in_left_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/slider_out_right_exit</item>
    </style>

    <!--  Material2主题    -->
    <style name="M2Theme"  parent="Theme.MaterialComponents.DayNight" >
        <item name="colorPrimary">@color/green_300</item>
    </style>

    <!--  BottomSheetDialog主题    -->
    <style name="BottomSheetDialog"  parent="Theme.Material3.DayNight.BottomSheetDialog" >
    </style>

    <!-- 可全屏dialog   -->
    <style name="CustomDialog1" parent="Theme.AppCompat.Light.Dialog">
        <!-- true（默认值）：对话框会以浮动窗口的形式显示，通常会有圆角和阴影效果，并且不会占满整个屏幕。-->
        <!-- false：对话框将作为一个普通的 Activity 窗口显示，可以占满整个屏幕或根据需要自定义大小。此时，它不会表现出浮动窗口的特性 -->
        <item name="android:windowIsFloating">false</item>
        <!-- dialog外点击关闭       -->
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!--  紧贴屏幕，消除padding    -->
        <item name="android:windowBackground">@color/transparent</item>
        <!--  背景是否透明    -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.3</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>
</resources>